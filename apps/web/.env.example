# SevenBoard Web 環境変数テンプレート
# 本番 (Vercel) では Project Settings → Environment Variables に登録。
# ローカルは apps/web/.env.local にコピー。

# API のベース URL。本番は Railway public URL。
# 末尾スラッシュなし。
NEXT_PUBLIC_API_URL=http://localhost:3001
